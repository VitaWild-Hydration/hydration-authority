---
interface Props {
  rank: number;
}
const { rank } = Astro.props;
---

<div class="inline-flex items-center justify-center w-16 h-16 md:w-20 md:h-20 rounded-md bg-ink-primary text-surface-card font-heading font-bold text-[2rem] md:text-[2.4rem] leading-none shadow-sm">
  #{rank}
</div>
